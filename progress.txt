# Google Sheets Backup Feature - Progress & Learnings

## Codebase Patterns

### Project Structure
- **Services:** `chibi/services/` - Business logic layer, inject dependencies via `__init__`
- **Database:** `chibi/database/` - Async SQLite with aiosqlite, repositories pattern
- **Cogs:** `chibi/cogs/` - Discord command handlers using discord.app_commands
- **Tools:** `chibi/tools/` - LangGraph tools, each has `tool.py` with Tool interface
- **Tests:** `tests/` - pytest with pytest-asyncio, mocks in `tests/mocks/`

### Async Patterns
- All service methods are async: `async def method_name(self) -> ReturnType:`
- Database operations use async context managers
- Discord command handlers are async
- Use `await` for all async calls
- pytest-asyncio mode is "auto" (see conftest.py)

### Database Patterns
- **Location**: `data/chibi.db` (SQLite database with student progress)
- **Schema Documentation**: See `DATABASE_STRUCTURE.md` for complete schema
- **Tables to backup**: users, quiz_attempts, concept_mastery, llm_quiz_attempts, attendance
- **Data Types**: BOOLEAN stored as 0/1, TIMESTAMP as ISO string, NULL as NULL
- Connection: `async with self.db.connection() as conn:`
- Transactions: `async with conn.execute("BEGIN"): ... await conn.commit()`
- Queries: `async with conn.execute(query, params) as cursor:`
- Schema defined in `chibi/database/connection.py` - see `_init_schema()` method
- Migrations: Adds review_status, student_id columns to existing databases
- Repositories inherit from BaseRepository
- Always use parameterized queries to prevent SQL injection
- For tests: Use `Database(":memory:")` for in-memory testing

### Service Patterns
- Located in `chibi/services/`
- Inject dependencies via `__init__(self, db: Database, config: Dict, ...)`
- Example: `QuizService(db, rag_service, llm_manager, config)`
- Return Dict or dataclass for structured data
- Raise custom exceptions, catch in cogs for user-friendly messages
- Add logging: `logger = logging.getLogger(__name__)`

### Discord Cog Patterns
- Located in `chibi/cogs/`
- Inherit from `commands.Cog`
- Use `@app_commands.command()` for slash commands
- Admin check: `@commands.has_permissions(administrator=True)`
- Ephemeral messages: `await interaction.response.defer(ephemeral=True)`
- Error handling: Catch exceptions, show user-friendly messages
- Registration: Add to `chibi/bot.py` in setup_hook or similar

### Testing Patterns
- Test files mirror source structure: `tests/test_[module].py`
- Fixtures in `conftest.py` - database, mock clients, etc.
- Mocks in `tests/mocks/` - Discord objects, LLM providers
- Use `@pytest.fixture` for reusable test setup
- Mark async tests with `@pytest.mark.asyncio` (auto mode)
- Use `@pytest.mark.skip` for tests before implementation
- Mock external APIs: `unittest.mock.patch()` or `unittest.mock.Mock()`
- In-memory database for tests: `Database(":memory:")`
- **Test Credentials**: `google-credentials.json` in project root for manual testing
  - Use ONLY for manual testing during development
  - Unit tests should ALWAYS mock Google API calls
  - Never use real production credentials in tests

### Configuration
- Main config: `config.yaml` - YAML format
- Structure: Top-level keys (bot, database, llm, similarity, backup)
- Access in code: `self.config.get('backup', {}).get('google_sheets', {})`
- Environment vars: Use `.env` for secrets (Discord token, API keys)
- Example section structure:
  ```yaml
  backup:
    google_sheets:
      enabled: true
      credentials_file: "credentials/google_oauth_credentials.json"
      token_file: "credentials/token.json"
  ```

### Git Patterns
- Branch naming: `ralph/[feature-name]`
- Commit message format: `[type]: [story-id] - [subject]`
- Types: feat, test, fix, chore, refactor, docs
- Always include: `Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>`
- Exclude from git: `credentials/*.json`, `data/`, `.env`

### Dependencies
- Package manager: `uv` (not pip)
- Install: `uv sync`
- Add dependency: Edit `pyproject.toml` then run `uv sync`
- Test imports: `python -c 'import package_name'`

### Running Tests
- All tests: `uv run pytest tests/ -v`
- Specific file: `uv run pytest tests/test_[component].py -v`
- With coverage: `uv run pytest tests/ --cov=chibi --cov-report=term-missing`
- Collect only (check structure): `uv run pytest tests/ --collect-only`

---

## Implementation Log

## 2026-01-11 - US-001, US-002, US-003 (Foundation Phase)
**What was implemented:** Complete foundation setup for backup feature
**Files changed:**
- chibi/backup/__init__.py (created)
- credentials/.gitignore (created)
- tests/integration/ (created)
- .gitignore (updated)
- pyproject.toml (updated with Google Sheets dependencies)
- config.yaml (added backup section)

**Learnings:**
- Directory structure: chibi/backup/ is the main module, credentials/ for OAuth files
- Git ignore pattern: credentials/.gitignore must be tracked to preserve directory in repo
- Dependencies: google-auth, google-auth-oauthlib, google-api-python-client, gspread
- Config structure: backup.google_sheets section with credentials_file, token_file, scopes
- OAuth scopes needed: spreadsheets and drive.file

**Verification:**
- ✓ All directories created and tracked in git
- ✓ Python imports work: `python -c 'import gspread; import google.auth'`
- ✓ Config loads correctly with backup section
- ✓ Credentials directory properly ignored by git

---

## 2026-01-11 - US-004
**What was implemented:** Unit tests for Google Sheets OAuth client
**Files changed:**
- tests/test_google_sheets_client.py (created - 487 lines, 20 test cases)
- uv.lock (updated with test dependencies)

**Test Coverage:**
- OAuth 2.0 authentication flow (4 tests): initialization, first-time auth, token refresh, token reuse
- Spreadsheet CRUD operations (6 tests): create, get, write, write with auto-sheet creation, read, list
- Error handling (3 tests): API failures, permission errors, network retries
- Batch operations (2 tests): large datasets with batching, rate limit handling with exponential backoff
- Additional edge cases (5 tests): missing credentials, token persistence, etc.

**Learnings:**
- **TDD approach:** Tests written BEFORE implementation exists - defines the contract
- **pytest.importorskip:** Use at module level to skip entire module if implementation missing
- **Mocking pattern:** Mock google.auth, gspread.authorize, and API responses
- **Test dependencies:** Must run `uv sync --extra test` to install pytest and related tools
- **Test structure:** Organize by feature area (OAuth, CRUD, Errors, Batch) using test classes
- **Skip markers:** Use `@pytest.mark.skip(reason="Implementation not yet created")` on all tests

**Verification:**
- ✓ pytest --collect-only succeeds (validates structure and imports)
- ✓ All tests properly skipped until implementation
- ✓ Comprehensive coverage of success and failure paths

---

## 2026-01-11 - US-005
**What was implemented:** Unit tests for SQLite to Google Sheets exporter
**Files changed:** tests/test_sheets_exporter.py (created - 667 lines, 18 test cases)

**Test Coverage:**
- Initialization and dependency injection (1 test)
- Full export orchestration with sample data (3 tests)
- Data transformation: SQLite rows to list-of-lists format (2 tests)
- Data type conversions: None→"", 0/1→TRUE/FALSE, text truncation to 50k chars (5 tests)
- Sheet creation: 6 sheets (Metadata, Users, QuizAttempts, ConceptMastery, LLMQuizAttempts, Attendance) (2 tests)
- Batch processing for large datasets (1 test)
- Error handling: API failures, database failures (3 tests)

**Learnings:**
- **Test organization:** Group tests by feature area using test classes
- **Data transformation testing:** Test SQLite row format to Sheets list-of-lists conversion
- **Type conversion patterns:**
  - NULL → "" (empty string) for Google Sheets compatibility
  - BOOLEAN (0/1) → "TRUE"/"FALSE" strings
  - TEXT fields → truncate to 50,000 chars (Sheets cell limit)
  - TIMESTAMP → preserve as ISO string format
- **Export structure:** 6 sheets required: Metadata (export info), 5 data tables
- **Batch processing:** Large datasets should be written in 1000-row batches
- **Metadata sheet:** Must include export_date, schema_version, and table counts
- **Mock patterns:** Mock GoogleSheetsClient with AsyncMock for async methods
- **Database testing:** Use test_database fixture from conftest.py (in-memory SQLite)
- **pytest.importorskip:** Place at module level to skip entire file until implementation exists

**Verification:**
- ✓ pytest --collect-only succeeds (validates structure)
- ✓ All tests properly skipped until implementation (US-010)
- ✓ Comprehensive coverage of all US-005 acceptance criteria
- ✓ No regressions in existing tests

---
## 2026-01-11 - US-006
**What was implemented:** Unit tests for Google Sheets to SQLite importer
**Files changed:** tests/test_sheets_importer.py (created - 781 lines, 18 test cases)

**Test Coverage:**
- Initialization and dependency injection (1 test)
- Schema validation (4 tests): valid schema, missing Metadata sheet, missing data sheet, invalid schema version
- Data type conversions (4 tests): empty→None, TRUE/FALSE→1/0, numeric strings→int/float, timestamp preservation
- Replace mode (2 tests): DELETE then INSERT, preserves foreign key relationships
- Merge mode (1 test): INSERT OR REPLACE updates existing and inserts new records
- Transaction rollback (2 tests): foreign key violations, preserves original data on error
- Spreadsheet URL extraction (3 tests): full URL, short URL, plain ID
- Full import workflow (1 test): returns summary with mode, counts, and status

**Learnings:**
- **Schema validation testing:** Test for missing sheets, invalid versions, and proper structure
- **Reverse transformations:** Import is opposite of export (empty→None, TRUE/FALSE→1/0)
- **Transaction testing:** Verify rollback preserves original data on any error
- **Foreign key testing:** Ensure referential integrity maintained during import
- **Mode testing patterns:**
  - Replace mode: DELETE all rows from tables, then INSERT new data
  - Merge mode: INSERT OR REPLACE for each row (updates by primary key)
- **URL extraction:** Parse Google Sheets URLs to extract spreadsheet ID from various formats
- **Import workflow:** Test end-to-end flow returns summary dict with counts
- **Mock patterns:** Use side_effect for read_sheet to return different data per sheet_name

**Verification:**
- ✓ pytest --collect-only succeeds (validates structure)
- ✓ All tests properly skipped until implementation (US-011)
- ✓ Comprehensive coverage of all US-006 acceptance criteria
- ✓ No regressions in existing tests

---

